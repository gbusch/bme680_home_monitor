// insert your credentials and rename the file to credentials.h

const char* ssid = ## wifi ssid ##;
const char* password = ## wifi password ##;
const char* mqttServer = ## address of mqtt broker ##;
const int mqttPort = 1883; // or other port of mqtt broker